import { ArrayFilterImpurePipe } from './array-filter-impure.pipe';

describe('ArrayFilterImpurePipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayFilterImpurePipe();
    expect(pipe).toBeTruthy();
  });
});
