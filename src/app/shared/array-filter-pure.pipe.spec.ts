import { ArrayFilterPurePipe } from './array-filter-pure.pipe';

describe('ArrayFilterPurePipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayFilterPurePipe();
    expect(pipe).toBeTruthy();
  });
});
